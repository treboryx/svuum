import { describe, expect, it } from "vitest";
import { parcel } from "../src";

describe.concurrent("isAfm", () => {
  it("XXXXXXX", async () => {
    await parcel("XXXXXXX").then(console.log);
  });
});
